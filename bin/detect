#!/usr/bin/env bash

if [ $(pushd $1 && npm run -l | grep "^  build$") ]; then
    echo "-----> Will run `npm run build`..."
    exit 0
else
    echo "-----> ! There is no `build` script"
    exit 1
fi
